{% schema %}
  {
    "name": "Section name",
    "settings": [
      {
      "type":"text",
        "id": "id",
        "label" : "Heading"
      }
    ],
    "blocks": [
      {
      "type":"text",
      "name": "text",
        "settings": [
          {
          "type":"text",
          "id": "id",
          "label" : "Heading"
        }
        ]
      },
      {
      "type":"Select",
      "name": "select",
        "settings": [
          {
          "type":"text",
          "id": "id",
          "label" : "Heading"
        }
        ]
      }
    ],
    "presets": [
      {
        "name": "Section name"
      }
    ]
  }
{% endschema %}